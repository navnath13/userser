version: 0.2

phases:
  build:
    commands:
      - echo "Entered the build phase..."
      - mvn clean package
    post_build:
      commands:
        - echo "Build completed successfully"
        - mv target/deploy ./

artifacts:
  files:
    - deplo/**/*
  discard-paths: yes